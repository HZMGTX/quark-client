package com.ghostclient.event.events;

import com.ghostclient.event.Event;

/**
 * Fired when the player attempts to jump.
 * Cancel to prevent the jump from occurring.
 */
public class EventJump extends Event {

    // cancellable by default (from base Event)
}
